---
'@octanejs/tanstack-pacer': patch
'@octanejs/tanstack-table': patch
---

Skip compiler-tagged children blocks in dual-mode `Subscribe` helpers so `.tsrx` element children render instead of being invoked as render props.
