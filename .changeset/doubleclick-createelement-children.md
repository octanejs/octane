---
"octane": patch
---

Map JSX-compatible `onDoubleClick` handlers to the native `dblclick` DOM event in both compiled and spread/de-opt event paths, and expose positional `createElement` children on `props.children` for host descriptors as well as components.
