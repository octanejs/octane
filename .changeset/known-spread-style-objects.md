---
'octane': patch
---

Allow trusted `knownAttributeSpreads` contracts to opt into signal-aware style objects with `style: 'object'`. Renderer-free bindings reuse the existing style capability, while contracts without this option retain CSS-text behavior.
