---
"octane": patch
---

Avoid redundant signal value adapters for immutable local values whose initializers are proven to return JavaScript primitives. Keep native read tracking, controlled input markers, and the existing potential signal capability for models imported after mount.
