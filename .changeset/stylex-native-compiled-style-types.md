---
'@octanejs/stylex': patch
---

Accept compiled StyleX styles with nested conditional pseudo-element values in native `sx` attributes, including forced-colors rules. Preserve rejection of raw style objects, uncalled style factories, and `true` values.
