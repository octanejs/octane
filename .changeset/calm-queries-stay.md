---
'@octanejs/tanstack-query': patch
---

Keep committed query content visible while a new query key suspends in a transition. Retain each key's promise through pending-cue replays without losing sequential query suspension.
