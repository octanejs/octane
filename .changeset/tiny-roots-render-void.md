---
'octane': patch
---

Specialize production function-local roots when every render uses a stable same-module compiled void component. Preserve generic roots for escaping roots, writable component bindings, unknown render targets, development and alternate renderer modes.
