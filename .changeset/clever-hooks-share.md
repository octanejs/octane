---
'octane': patch
---

Infer omitted dependency arrays for locally declared custom hooks in
full-compiled `.tsrx`/`.tsx` modules that transparently forward their callback
and final dependency parameter to a supported hook.
