---
"octane": patch
---

Create runtime adapter hosts, including Motion SVG elements, in their destination's SVG or MathML namespace. Preserve case-sensitive SVG attributes and HTML children inside `foreignObject` or HTML-encoded MathML `annotation-xml` elements.
