---
'@octanejs/blocknote': patch
---

Implement a private Octane adapter for `@blocknote/core` with a
headless `BlockNoteView`, editor context and hooks, lifecycle cleanup, event
subscriptions, editability, and color-scheme handling. Remove the unpublished
React UI staging tree from the package. Keep publication blocked while the
provenance of two retained mechanical-port context/hook files is resolved.
