name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
    types: [opened, reopened, synchronize, ready_for_review, converted_to_draft, closed]

concurrency:
  # A merged pull request changes github.ref to main on its closed event. Keep
  # pull request runs grouped by number so closing one cancels only its CI run,
  # while the merge's new main run proceeds in its own ref-based group.
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  # Named once: the sharded suite excludes these specs and website_e2e runs
  # them. Splitting the paths means a stale name could silently stop running
  # one, so website_e2e asserts every file exists before it starts.
  WEBSITE_DOCS_SPEC: website/tests/core-apis-docs.test.ts
  WEBSITE_E2E_SPEC: website/tests/ssr-hydration.e2e.test.ts
  WEBSITE_SSR_SPEC: website/tests/ssr-smoke.test.ts
  ASTRO_E2E_SPEC: packages/astro/tests/astro.e2e.test.ts

jobs:
  release_change:
    name: classify changeset release
    # GitHub still emits workflow events while a pull request is a draft. Keep
    # every expensive job behind this guard. The two tiny required-check
    # reporters below still run so branch protection does not remain pending.
    if: ${{ github.event.action != 'closed' && (github.event_name != 'pull_request' || github.event.pull_request.draft == false) }}
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      pull-requests: read
    outputs:
      is_release: ${{ steps.classify.outputs.is_release }}
      is_release_pr: ${{ steps.classify.outputs.is_release_pr }}
      is_release_merge: ${{ steps.classify.outputs.is_release_merge }}
      full_ci: ${{ steps.verify_scope.outputs.full_ci }}

    steps:
      # Authenticate both a generated release PR event and the eventual main
      # merge from PR metadata, commit ancestry, authorship, and changed files.
      # Any missing or unexpected data falls back to normal CI.
      - name: Identify a generated Changesets release change
        id: classify
        uses: actions/github-script@ed597411d8f924073f98dfc5c65a23a2325f34cd # v8
        with:
          script: |
            core.setOutput("is_release", "false");
            core.setOutput("is_release_pr", "false");
            core.setOutput("is_release_merge", "false");

            try {
              const { owner, repo } = context.repo;
              const expectedPull = (pull) =>
                pull.title === "Version Packages" &&
                pull.user?.login === "github-actions[bot]" &&
                pull.base.ref === "main" &&
                pull.base.repo?.full_name === `${owner}/${repo}` &&
                pull.head.ref === "changeset-release/main" &&
                pull.head.repo?.full_name === `${owner}/${repo}`;

              let pull;
              let generatedSha;
              let expectedParentSha;
              let merged;
              let releaseKind;

              if (context.eventName === "pull_request") {
                const candidate = context.payload.pull_request;
                if (!candidate || !expectedPull(candidate)) {
                  return;
                }
                pull = candidate;
                generatedSha = candidate.head.sha;
                expectedParentSha = candidate.base.sha;
                releaseKind = "pr";
              } else if (context.eventName === "push") {
                const message = context.payload.head_commit?.message ?? "";
                const commits = context.payload.commits ?? [];
                if (
                  context.ref !== "refs/heads/main" ||
                  !message.startsWith("Version Packages") ||
                  commits.length !== 1
                ) {
                  return;
                }

                const { data: pulls } =
                  await github.rest.repos.listPullRequestsAssociatedWithCommit({
                    owner,
                    repo,
                    commit_sha: context.sha,
                  });
                const matches = pulls.filter(
                  (candidate) =>
                    candidate.merged_at !== null &&
                    candidate.merge_commit_sha === context.sha &&
                    expectedPull(candidate),
                );
                if (matches.length !== 1) {
                  core.warning(
                    `Expected one generated Changesets PR for ${context.sha}; found ${matches.length}. Running the full CI suite.`,
                  );
                  return;
                }

                pull = matches[0];
                generatedSha = pull.head.sha;
                releaseKind = "merge";
              } else {
                return;
              }

              const releaseFiles = await github.paginate(
                github.rest.pulls.listFiles,
                {
                  owner,
                  repo,
                  pull_number: pull.number,
                  per_page: 100,
                },
              );
              const unexpectedFiles = releaseFiles.filter((file) => {
                if (
                  file.status === "removed" &&
                  /^\.changeset\/[^/]+\.md$/.test(file.filename)
                ) {
                  return false;
                }
                if (file.status !== "added" && file.status !== "modified") {
                  return true;
                }
                return !(
                  file.filename.endsWith("/CHANGELOG.md") ||
                  file.filename.endsWith("/package.json") ||
                  file.filename === "docs/bindings-status.md" ||
                  file.filename === "docs/packages.md" ||
                  file.filename === "packages/octane/src/version.ts" ||
                  file.filename.startsWith("packages/shadcn/registry/") ||
                  file.filename === "packages/cli/src/data/octane-data.json" ||
                  file.filename ===
                    "packages/octane-evals/datasets/train/user-apps-v1/manifest.jsonl"
                );
              });

              if (releaseFiles.length === 0 || unexpectedFiles.length > 0) {
                const sample = unexpectedFiles
                  .slice(0, 5)
                  .map((file) => file.filename)
                  .join(", ");
                core.warning(
                  `Changesets PR #${pull.number} contains unexpected files${sample ? `: ${sample}` : ""}. Running the full CI suite.`,
                );
                return;
              }

              const { data: generated } = await github.rest.repos.getCommit({
                owner,
                repo,
                ref: generatedSha,
              });
              const expectedReleaseCommit =
                generated.commit.message === "Version Packages" &&
                generated.commit.author?.email ===
                  "41898282+github-actions[bot]@users.noreply.github.com" &&
                generated.commit.committer?.email ===
                  "41898282+github-actions[bot]@users.noreply.github.com";

              if (!expectedReleaseCommit) {
                core.warning(
                  `Changesets PR #${pull.number} does not have the expected generated commit. Running the full CI suite.`,
                );
                return;
              }

              if (releaseKind === "pr") {
                const basedOnCurrentMain =
                  generated.parents.length === 1 &&
                  generated.parents[0].sha === expectedParentSha;
                if (!basedOnCurrentMain) {
                  core.warning(
                    `Changesets PR #${pull.number} is not based directly on its current main base. Running the full CI suite.`,
                  );
                  return;
                }

                core.setOutput("is_release", "true");
                core.setOutput("is_release_pr", "true");
                core.notice(
                  `Authenticated generated Changesets PR #${pull.number}; skipping duplicate PR CI.`,
                );
                return;
              }

              ({ data: merged } = await github.rest.repos.getCommit({
                owner,
                repo,
                ref: context.sha,
              }));
              const sameTree =
                merged.commit.tree.sha === generated.commit.tree.sha;
              const sameParent =
                merged.parents.length === 1 &&
                generated.parents.length === 1 &&
                merged.parents[0].sha === generated.parents[0].sha;
              if (!sameTree || !sameParent) {
                core.warning(
                  `Changesets merge ${context.sha} does not match its generated PR commit. Running the full CI suite.`,
                );
                return;
              }

              const parentSha = merged.parents[0].sha;
              const { data: parentRuns } =
                await github.rest.actions.listWorkflowRuns({
                  owner,
                  repo,
                  workflow_id: "ci.yml",
                  branch: "main",
                  event: "push",
                  status: "completed",
                  head_sha: parentSha,
                  per_page: 10,
                });
              const requiredTestJobs = ["test (22)", "test (24)"];
              let parentPassedTests = false;

              for (const run of parentRuns.workflow_runs) {
                if (
                  run.head_sha !== parentSha ||
                  run.head_branch !== "main" ||
                  run.head_repository?.full_name !== `${owner}/${repo}` ||
                  run.event !== "push" ||
                  typeof run.head_commit?.message !== "string" ||
                  run.head_commit.message.startsWith("Version Packages")
                ) {
                  continue;
                }

                const jobs = await github.paginate(
                  github.rest.actions.listJobsForWorkflowRun,
                  { owner, repo, run_id: run.id, per_page: 100 },
                );
                const successfulJobs = new Set(
                  jobs
                    .filter((job) => job.conclusion === "success")
                    .map((job) => job.name),
                );
                if (requiredTestJobs.every((name) => successfulJobs.has(name))) {
                  parentPassedTests = true;
                  break;
                }
              }

              if (!parentPassedTests) {
                core.warning(
                  `The parent of Changesets merge ${context.sha} has no completed CI run with the full test suite passing. Running the full tests.`,
                );
                return;
              }

              core.setOutput("is_release", "true");
              core.setOutput("is_release_merge", "true");
            } catch (error) {
              core.warning(
                `Could not authenticate Changesets release ${context.sha}: ${error.message}. Running the full CI suite.`,
              );
            }

      - name: Checkout change history
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          fetch-depth: 0

      # Keep the cheap lint/static gate universal. Expensive matrices can be
      # omitted only when the committed classifier proves that every changed
      # file is documentation, metadata, or an isolated developer helper.
      - name: Classify the CI scope
        id: change_scope
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.before }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        run: >-
          node scripts/classify-ci-change.mjs
          --base "$BASE_SHA"
          --head "$HEAD_SHA"
          --github-output "$GITHUB_OUTPUT"

      # A lightweight diff can inherit coverage only from a base commit that
      # actually completed every expensive lane. This prevents a quick metadata
      # push from cancelling an untested source push and then going green.
      - name: Verify full-suite provenance
        id: verify_scope
        uses: actions/github-script@ed597411d8f924073f98dfc5c65a23a2325f34cd # v8
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.before }}
          CANDIDATE_FULL_CI: ${{ steps.change_scope.outputs.full_ci }}
        with:
          script: |
            core.setOutput("full_ci", "true");
            if (process.env.CANDIDATE_FULL_CI !== "false") {
              return;
            }

            try {
              const { owner, repo } = context.repo;
              const baseSha = process.env.BASE_SHA;
              if (!baseSha || /^0+$/.test(baseSha)) {
                core.warning(
                  "The lightweight change has no comparable base commit. Running full CI.",
                );
                return;
              }

              const requiredFullJobs = [
                "website integration suite",
                "React parity checks",
                "typecheck",
                "validate publishable packages",
                ...["package-builds", "eval-corpus", "browser"].map(
                  (lane) => `heavy integration (${lane})`,
                ),
                ...["1/3", "2/3", "3/3"].map(
                  (shard) => `example apps (${shard})`,
                ),
                ...["minimum r156", "current"].map(
                  (lane) => `Three compatibility (${lane})`,
                ),
                ...["minimum", "current"].map(
                  (lane) => `Lynx compatibility (${lane})`,
                ),
                ...[22, 24].flatMap((node) =>
                  ["1/4", "2/4", "3/4", "4/4"].map(
                    (shard) => `test shard (Node ${node}, ${shard})`,
                  ),
                ),
              ];
              const { data: baseRuns } =
                await github.rest.actions.listWorkflowRuns({
                  owner,
                  repo,
                  workflow_id: "ci.yml",
                  branch: "main",
                  event: "push",
                  status: "completed",
                  head_sha: baseSha,
                  per_page: 10,
                });

              for (const run of baseRuns.workflow_runs) {
                if (
                  run.head_sha !== baseSha ||
                  run.head_branch !== "main" ||
                  run.head_repository?.full_name !== `${owner}/${repo}` ||
                  run.event !== "push" ||
                  run.conclusion !== "success"
                ) {
                  continue;
                }

                const jobs = await github.paginate(
                  github.rest.actions.listJobsForWorkflowRun,
                  { owner, repo, run_id: run.id, per_page: 100 },
                );
                const successfulJobs = new Set(
                  jobs
                    .filter((job) => job.conclusion === "success")
                    .map((job) => job.name),
                );
                const hasReusableProvenance =
                  successfulJobs.has("CI provenance");
                const completedFullSuite = requiredFullJobs.every((job) =>
                  successfulJobs.has(job),
                );
                if (hasReusableProvenance || completedFullSuite) {
                  core.setOutput("full_ci", "false");
                  core.notice(
                    `Skipping expensive CI for a lightweight change based on tested ${baseSha}.`,
                  );
                  return;
                }
              }

              core.notice(
                `Base ${baseSha} has no completed run with every expensive lane passing. Running full CI.`,
              );
            } catch (error) {
              core.warning(
                `Could not verify full-suite provenance: ${error.message}. Running full CI.`,
              );
            }

  test_shard:
    name: test shard (Node ${{ matrix.node-version }}, ${{ matrix.shard }})
    needs: release_change
    # Each source change has already passed this matrix before Changesets
    # generates its metadata-only release commit. Keep the post-merge CI run
    # (publishing waits for it), but do not repeat the full suite once the merge
    # has been authenticated above.
    if: ${{ always() && !cancelled() && github.event.action != 'closed' && (github.event_name != 'pull_request' || github.event.pull_request.draft == false) && needs.release_change.outputs.is_release != 'true' && needs.release_change.outputs.full_ci != 'false' }}
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: ["22.22.2", 24]
        # Keep the full supported-Node coverage while splitting the large
        # Vitest suite across runners. Four shards cuts the CI critical path
        # substantially without letting repeated global setup dominate.
        shard: ["1/4", "2/4", "3/4", "4/4"]

    steps:
      - name: Checkout code
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6

      - name: Install pnpm
        uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: ${{ matrix.node-version }}
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --prod false --frozen-lockfile

      # Real builds, packed consumers, and browser suites run once on Node 24 in
      # the dedicated integration jobs below. Keep both supported Nodes on the
      # ordinary suite without making time-sensitive integration work compete
      # with hundreds of unrelated files.
      - name: Run tests
        # WEBSITE_DOCS_SPEC is excluded through the environment, not `--exclude`:
        # it lives in the `website-unit` project, and a project that declares its
        # own `exclude` makes Vitest ignore the CLI flag. The flag was here and
        # did nothing. See the `website-unit` project in vitest.config.js.
        env:
          OCTANE_EXCLUDE_WEBSITE_DOCS: "1"
        run: >-
          pnpm test
          --config vitest.ci-sharded.config.js
          --shard=${{ matrix.shard }}
          --exclude "$WEBSITE_E2E_SPEC"
          --exclude "$WEBSITE_SSR_SPEC"
          --exclude "website-mcp/tests/built-handler.e2e.test.ts"
          --exclude "packages/octane-evals/tests/user-app-corpus.test.ts"
          --exclude "packages/rspeedy-plugin-octane/tests/packed-consumer.test.ts"
          --exclude "packages/rspeedy-plugin-octane/tests/demo.test.ts"
          --exclude "packages/rspeedy-plugin-octane/tests/browser/**/*.test.ts"
          --exclude "packages/rsbuild-plugin-octane/tests/rsbuild.integration.test.ts"
          --exclude "packages/vite-plugin-octane/tests/production.test.ts"
          --exclude "packages/octane/tests/browser/**/*.test.ts"
          --exclude "packages/dexie/tests/browser/**/*.test.ts"
          --exclude "packages/tiptap/tests/browser/**/*.test.ts"
          --exclude "packages/three/tests/browser/**/*.test.ts"
          --exclude "$ASTRO_E2E_SPEC"

  react_parity_checks:
    name: React parity checks
    needs: release_change
    # The complete package suite is intentionally Node 24-only. Like the
    # general shards, an authenticated release or lightweight change inherits
    # the last successful full run through CI provenance.
    if: ${{ always() && !cancelled() && github.event.action != 'closed' && (github.event_name != 'pull_request' || github.event.pull_request.draft == false) && needs.release_change.outputs.is_release != 'true' && needs.release_change.outputs.full_ci != 'false' }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6

      - name: Install pnpm
        uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6

      - name: Setup Node.js
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: 24
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --prod false --frozen-lockfile

      # The manifest runner executes every required runtime and type lane. Each
      # runner's JSON report proves the exact identities without a separate
      # pre-execution Vitest collection pass.
      - name: Check React parity inventories and execute required lanes
        run: pnpm react-parity:check

  # Keep the existing required check names while the work happens in parallel
  # above. `always()` makes a failed shard produce a failed required check and
  # folds the dedicated integration suites into the same merge and publishing
  # gates. An authenticated release or lightweight-change skip must satisfy
  # every prerequisite.
  test:
    name: test (${{ matrix.required-context }})
    needs: [release_change, test_shard, react_parity_checks, website_e2e, heavy_integration]
    # The release PR does not need test contexts. The post-merge release run does:
    # publish.yml verifies these stable aggregate names before publishing.
    if: ${{ always() && !cancelled() && github.event.action != 'closed' && (github.event_name != 'pull_request' || github.event.pull_request.draft == false) && needs.release_change.outputs.is_release_pr != 'true' }}
    runs-on: ubuntu-latest

    strategy:
      matrix:
        # Keep one stable aggregate context for each supported Node release.
        required-context: [22, 24]

    steps:
      - name: Verify all test suites passed
        env:
          FULL_CI: ${{ needs.release_change.outputs.full_ci }}
          IS_CHANGESET_RELEASE_MERGE: ${{ needs.release_change.outputs.is_release_merge }}
          SHARD_RESULT: ${{ needs.test_shard.result }}
          REACT_PARITY_RESULT: ${{ needs.react_parity_checks.result }}
          WEBSITE_E2E_RESULT: ${{ needs.website_e2e.result }}
          HEAVY_INTEGRATION_RESULT: ${{ needs.heavy_integration.result }}
        run: |
          if [ "$IS_CHANGESET_RELEASE_MERGE" = true ] || [ "$FULL_CI" = false ]; then
            test "$SHARD_RESULT" = skipped
            test "$REACT_PARITY_RESULT" = skipped
            test "$WEBSITE_E2E_RESULT" = skipped
            test "$HEAVY_INTEGRATION_RESULT" = skipped
          else
            test "$SHARD_RESULT" = success
            test "$REACT_PARITY_RESULT" = success
            test "$WEBSITE_E2E_RESULT" = success
            test "$HEAVY_INTEGRATION_RESULT" = success
          fi

  website_e2e:
    name: website integration suite
    needs: release_change
    # Same authenticated-release skip as the shards above: a metadata-only
    # Changesets merge has already passed this on its parent commit.
    if: ${{ always() && !cancelled() && github.event.action != 'closed' && (github.event_name != 'pull_request' || github.event.pull_request.draft == false) && needs.release_change.outputs.is_release != 'true' && needs.release_change.outputs.full_ci != 'false' }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6

      - name: Install pnpm
        uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6

      - name: Setup Node.js
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: 24
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --prod false --frozen-lockfile

      - name: Install Playwright Chromium
        run: pnpm --filter website exec playwright install --with-deps chromium

      # The sharded suite excludes these paths by name. If any one ever moves,
      # fail instead of silently losing its dedicated coverage.
      - name: Verify the excluded specs exist
        run: |
          test -f "$WEBSITE_DOCS_SPEC"
          test -f "$WEBSITE_SSR_SPEC"
          test -f "$WEBSITE_E2E_SPEC"

      - name: Run the website integration suite
        run: >-
          pnpm vitest run
          "$WEBSITE_DOCS_SPEC"
          "$WEBSITE_SSR_SPEC"
          "$WEBSITE_E2E_SPEC"
          --maxWorkers=1

  heavy_integration:
    name: heavy integration (${{ matrix.lane }})
    needs: release_change
    # Run build and browser coverage once on the newest supported Node. The
    # ordinary suite still runs on both Node 22 and 24 above.
    if: ${{ always() && !cancelled() && github.event.action != 'closed' && (github.event_name != 'pull_request' || github.event.pull_request.draft == false) && needs.release_change.outputs.is_release != 'true' && needs.release_change.outputs.full_ci != 'false' }}
    runs-on: ubuntu-latest

    strategy:
      matrix:
        include:
          - lane: package-builds
            chromium: true
            specs: >-
              website-mcp/tests/built-handler.e2e.test.ts
              packages/rspeedy-plugin-octane/tests/packed-consumer.test.ts
              packages/rspeedy-plugin-octane/tests/demo.test.ts
              packages/rspeedy-plugin-octane/tests/browser/web-host.test.ts
              packages/rsbuild-plugin-octane/tests/rsbuild.integration.test.ts
              packages/vite-plugin-octane/tests/production.test.ts
          - lane: eval-corpus
            chromium: false
            specs: packages/octane-evals/tests/user-app-corpus.test.ts
          - lane: browser
            chromium: true
            specs: >-
              packages/octane/tests/browser
              packages/dexie/tests/browser
              packages/tiptap/tests/browser
              packages/three/tests/browser
          - lane: astro
            chromium: true
            specs: packages/astro/tests/astro.e2e.test.ts
          - lane: runtime-loaders
            bun: true
            chromium: false
            specs: packages/octane/tests/register-hook.test.ts

    steps:
      - name: Checkout code
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6

      - name: Install pnpm
        uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6

      - name: Setup Node.js
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: 24
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --prod false --frozen-lockfile

      - name: Setup Bun
        if: ${{ matrix.bun }}
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: 1.3.14

      - name: Install Playwright Chromium
        if: ${{ matrix.chromium && matrix.lane != 'astro' }}
        run: pnpm --filter website exec playwright install --with-deps chromium

      - name: Install Playwright Chromium (astro)
        if: ${{ matrix.lane == 'astro' }}
        run: pnpm --filter @octanejs/astro exec playwright install --with-deps chromium

      - name: Run ${{ matrix.lane }} integration tests
        run: pnpm vitest run ${{ matrix.specs }} --maxWorkers=1

      - name: Run Bun compiler-preload integration test
        if: ${{ matrix.bun }}
        env:
          BUN_BINARY: bun
        run: node --test packages/octane/tests/register-hook-bun.integration.test.mjs

  lint_checks:
    name: lint checks
    needs: release_change
    if: ${{ always() && !cancelled() && github.event.action != 'closed' && (github.event_name != 'pull_request' || github.event.pull_request.draft == false) && needs.release_change.outputs.is_release_pr != 'true' }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          # The production-error catalog is append-only. Its check compares the
          # current tree with the PR base or previous main commit.
          fetch-depth: 0

      - name: Install pnpm
        uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6

      - name: Setup Node.js
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: 24
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Check formatting
        run: pnpm format:check

      # Two-layer sync check for the RuleSync-generated agent files. The root
      # `prepare` script already re-ran `rulesync generate || true` during
      # install, so if the committed files were stale they are now MODIFIED in
      # the working tree, and `git diff` is what actually catches that. The
      # explicit `rules:check` still matters for the case where prepare's
      # rulesync invocation failed silently (it is `|| true` by design).
      #
      # The diff is deliberately unfiltered. An enumerated path list silently
      # stops covering whatever RuleSync starts emitting next, which is exactly
      # how the skill and instruction outputs escaped it. `prepare` runs nothing
      # but rulesync, so a dirty tree here is a stale generated file.
      - name: Check AI rule files are in sync
        run: |
          pnpm rules:check
          git diff --exit-code

      # 0.x alpha: changesets must stay on the patch track. release/publish
      # already enforce this, but checking on PRs surfaces a stray major/minor
      # bump at review time instead of at release time.
      - name: Disallow major and minor bumps in changesets
        run: pnpm changeset:check

      - name: Validate CI workflow behavior
        run: pnpm ci:workflow:test

      # A committed test must execute and pass. Intentional framework
      # divergences are ordinary assertions; missing environment prerequisites
      # fail with setup instructions instead of silently turning CI green.
      - name: Disallow skipped and expected-failure tests
        run: pnpm test:markers:check

      # Packages ship their `src/` directly, so an ambient `declare module
      # '*.tsrx'` in one reaches consumers and turns THEIR .tsrx imports into
      # `any`. Benchmarks, playground apps, and test directories are unpublished
      # and stay allowed.
      - name: Disallow wildcard .tsrx declarations in published sources
        run: pnpm tsrx-decls:check

      # The root rule and every skill description load on EVERY agent session.
      # Path-scoped rules under .rulesync carry `globs` and load only when a
      # matching file is opened, so they are deliberately not budgeted.
      - name: Check always-loaded agent context is within budget
        run: pnpm context:budget:check

      # docs/parity-gaps.md audits executable expected-failure pins and should
      # remain at zero under the committed-test policy above.
      - name: Check parity-gap index is current
        run: pnpm parity:gaps:check

      # Keep metadata, generated coverage, public claims, and the parity
      # tooling itself covered on lightweight changes. Full lane execution
      # remains in the dedicated Node 24 job above.
      - name: Validate React parity metadata and public claims
        run: |
          pnpm react-parity:test
          pnpm react-parity:validate

      # Redact's issues, pull requests, and tests are mined at a pinned commit
      # into an authored contract ledger. This network-free gate validates its
      # dispositions/local evidence and keeps the generated report current.
      - name: Check Redact-derived adversarial contract ledger
        env:
          OCTANE_REDACT_AUDIT_BASE: ${{ github.event.pull_request.base.sha || github.event.before }}
        run: pnpm redact-audit:check

      # Production error URLs are a public compatibility surface. Keep the
      # append-only catalog, generated client/server formatters, and literal
      # runtime call sites synchronized before a package can be built.
      - name: Check Octane production error catalog
        env:
          OCTANE_ERROR_CODES_BASE: ${{ github.event.pull_request.base.sha || github.event.before }}
        run: pnpm error-codes:check

      # Package discovery is shared by every package-wide repository check.
      # This generated inventory catches unreviewed package additions and stale
      # hard-coded package counts before they spread to docs and tooling.
      - name: Check package inventory is current
        run: pnpm packages:inventory:check

      - name: Check binding parity-gap index is current
        run: pnpm binding-parity:gaps:check

      # @octanejs/cli ships a snapshot of the binding and error-code catalogs
      # because it runs against user projects, not this checkout. Regenerating
      # has to happen in the same change that edits a status.json or adds an
      # error code, or `octane bindings`/`octane explain` go stale on npm.
      - name: Check CLI data snapshot is current
        run: pnpm cli:data:check

      # The CLI page's file trees and the create-octane README describe what
      # `octane create` writes. The snapshot they are drawn from comes from
      # running the command, so adding a file to a template without regenerating
      # leaves both documents quietly describing the old project.
      - name: Check scaffold file manifest is current
        run: pnpm scaffold:manifest:check

      # docs/bindings-status.md is generated from the per-package status.json
      # blocks in the @octanejs/* binding packages. Changing a binding's scope
      # means updating its status.json and regenerating in the same change.
      - name: Check bindings-status table is current
        run: pnpm bindings:status:check

      - name: Check Lucide generated sources are current
        run: pnpm lucide:generate:check

      - name: Check Phosphor Icons generated sources are current
        run: pnpm phosphor-icons:generate:check

      # The shadcn registry is what the upstream CLI installs from, and it
      # embeds the sibling versions its components need. Regenerating happens at
      # `changeset:version` too, so this catches a source edit that never ran
      # the generator.
      - name: Check shadcn registry is current
        run: pnpm shadcn:registry:check

      # The publish build otherwise only runs at prepack, so a broken dist
      # (an entry point missing from the build, an unresolvable relative
      # import in dist/ or the verbatim-copied compiler/) would ship silently.
      # build.mjs ends with verify-dist.mjs: every emitted module's imports
      # must resolve and every publishConfig export must import in plain Node.
      - name: Build octane package and verify dist
        run: pnpm build

  typecheck_checks:
    name: typecheck checks
    needs: release_change
    if: ${{ always() && !cancelled() && github.event.action != 'closed' && (github.event_name != 'pull_request' || github.event.pull_request.draft == false) && needs.release_change.outputs.is_release_pr != 'true' && needs.release_change.outputs.full_ci != 'false' }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6

      - name: Install pnpm
        uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6

      - name: Setup Node.js
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: 24
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Type check
        run: pnpm typecheck

      - name: Validate example catalog and orchestration
        run: pnpm examples:static:check

  # `lint` and `typecheck` are protected-branch contexts. GitHub leaves a
  # required context pending when every job with that name is skipped, so keep
  # these reporters cheap and let the real work above remain skipped on drafts.
  lint:
    name: lint
    needs: [release_change, lint_checks]
    if: ${{ always() && !cancelled() && github.event.action != 'closed' }}
    runs-on: ubuntu-latest

    steps:
      - name: Report lint status
        env:
          IS_DRAFT: ${{ github.event_name == 'pull_request' && github.event.pull_request.draft == true }}
          IS_RELEASE_PR: ${{ needs.release_change.outputs.is_release_pr }}
          RELEASE_CHANGE_RESULT: ${{ needs.release_change.result }}
          LINT_CHECKS_RESULT: ${{ needs.lint_checks.result }}
        run: |
          if [ "$IS_DRAFT" = true ] || [ "$IS_RELEASE_PR" = true ]; then
            test "$LINT_CHECKS_RESULT" = skipped
          else
            test "$RELEASE_CHANGE_RESULT" = success
            test "$LINT_CHECKS_RESULT" = success
          fi

  typecheck:
    name: typecheck
    needs: [release_change, typecheck_checks]
    if: ${{ always() && !cancelled() && github.event.action != 'closed' }}
    runs-on: ubuntu-latest

    steps:
      - name: Report typecheck status
        env:
          IS_DRAFT: ${{ github.event_name == 'pull_request' && github.event.pull_request.draft == true }}
          IS_RELEASE_PR: ${{ needs.release_change.outputs.is_release_pr }}
          FULL_CI: ${{ needs.release_change.outputs.full_ci }}
          RELEASE_CHANGE_RESULT: ${{ needs.release_change.result }}
          TYPECHECK_CHECKS_RESULT: ${{ needs.typecheck_checks.result }}
        run: |
          if [ "$IS_DRAFT" = true ] || [ "$IS_RELEASE_PR" = true ] || [ "$FULL_CI" = false ]; then
            test "$TYPECHECK_CHECKS_RESULT" = skipped
          else
            test "$RELEASE_CHANGE_RESULT" = success
            test "$TYPECHECK_CHECKS_RESULT" = success
          fi

  example_shard:
    name: example apps (${{ matrix.shard }})
    needs: release_change
    if: ${{ always() && !cancelled() && github.event.action != 'closed' && (github.event_name != 'pull_request' || github.event.pull_request.draft == false) && needs.release_change.outputs.is_release_pr != 'true' && needs.release_change.outputs.full_ci != 'false' }}
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        # Keep every app internally serial while spreading complete app
        # lifecycles across runners. The planner weights declared journeys and
        # repeated execution modes, then assigns each app to exactly one shard.
        shard: ["1/3", "2/3", "3/3"]

    steps:
      - name: Checkout code
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6

      - name: Install pnpm
        uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6

      - name: Setup Node.js
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: 24
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --prod false --frozen-lockfile

      - name: Install Playwright Chromium
        run: pnpm --filter hacker-news-example exec playwright install --with-deps chromium

      - name: Run example browser journeys
        run: pnpm examples:e2e -- --shard=${{ matrix.shard }}

  # Keep browser journeys behind their own stable aggregate check so the
  # protected `typecheck` context can finish as soon as typechecking does.
  # `always()` makes a failed shard produce a failed example check, not a skip.
  examples:
    name: examples
    needs: [release_change, example_shard]
    if: ${{ always() && !cancelled() && github.event.action != 'closed' && (github.event_name != 'pull_request' || github.event.pull_request.draft == false) && needs.release_change.outputs.is_release_pr != 'true' }}
    runs-on: ubuntu-latest

    steps:
      - name: Verify all example shards passed
        env:
          FULL_CI: ${{ needs.release_change.outputs.full_ci }}
          EXAMPLE_SHARD_RESULT: ${{ needs.example_shard.result }}
        run: |
          if [ "$FULL_CI" = false ]; then
            test "$EXAMPLE_SHARD_RESULT" = skipped
          else
            test "$EXAMPLE_SHARD_RESULT" = success
          fi

  three_compat:
    name: Three compatibility (${{ matrix.lane }})
    needs: release_change
    if: ${{ always() && !cancelled() && github.event.action != 'closed' && (github.event_name != 'pull_request' || github.event.pull_request.draft == false) && needs.release_change.outputs.is_release_pr != 'true' && needs.release_change.outputs.full_ci != 'false' }}
    runs-on: ubuntu-latest
    # This matrix intentionally rewrites one importer without changing the
    # repository lockfile after the frozen base install.
    env:
      PNPM_CONFIG_VERIFY_DEPS_BEFORE_RUN: "false"

    strategy:
      fail-fast: false
      matrix:
        include:
          - lane: minimum r156
            version: 0.156.0
          - lane: current
            version: latest

    steps:
      - name: Checkout code
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6

      - name: Install pnpm
        uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6

      - name: Setup Node.js
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: 24
          cache: "pnpm"

      - name: Install pinned repository dependencies
        run: pnpm install --frozen-lockfile

      # Keep the r172 differential oracle immutable. These lanes replace only
      # the Three runtime/type pair, then select the Octane-owned compatibility
      # suite so current/minimum support cannot be faked by the pinned oracle.
      - name: Install Three ${{ matrix.version }} compatibility pair
        run: |
          THREE_VERSION="$(pnpm view three@${{ matrix.version }} version)"
          THREE_RELEASE_LINE="${THREE_VERSION%.*}"
          pnpm --filter @octanejs/three add --save-dev --lockfile=false "three@$THREE_VERSION" "@types/three@${THREE_RELEASE_LINE}.x"
          echo "OCTANE_THREE_COMPAT_VERSION=$THREE_VERSION" >> "$GITHUB_ENV"

      - name: Verify resolved Three compatibility pair
        run: |
          THREE_VERSION="$(node -e "process.stdout.write(require('./packages/three/node_modules/three/package.json').version)")"
          TYPES_VERSION="$(node -e "process.stdout.write(require('./packages/three/node_modules/@types/three/package.json').version)")"
          THREE_RELEASE_LINE="${THREE_VERSION%.*}"
          TYPES_RELEASE_LINE="${TYPES_VERSION%.*}"
          echo "three=$THREE_VERSION @types/three=$TYPES_VERSION"
          test "$THREE_RELEASE_LINE" = "$TYPES_RELEASE_LINE"

      - name: Typecheck Three source and public API
        run: |
          pnpm exec tsgo --noEmit -p packages/three/tsconfig.json
          pnpm exec tsc --noEmit -p packages/three/typetests/tsconfig.json

      - name: Run Three compatibility behavior tests
        run: pnpm --dir packages/three test:compat

  lynx_compat:
    name: Lynx compatibility (${{ matrix.lane }})
    needs: release_change
    if: ${{ always() && !cancelled() && github.event.action != 'closed' && (github.event_name != 'pull_request' || github.event.pull_request.draft == false) && needs.release_change.outputs.is_release_pr != 'true' && needs.release_change.outputs.full_ci != 'false' }}
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        include:
          - lane: minimum
          - lane: current

    steps:
      - name: Checkout code
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6

      - name: Install pnpm
        uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6

      - name: Setup Node.js
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: 24
          cache: "pnpm"

      - name: Install pinned repository dependencies
        run: pnpm install --frozen-lockfile

      # The script packs the four Octane packages, installs one exact Lynx
      # graph into a temporary external consumer without a lockfile, verifies
      # physical dependency identity, and performs two clean native builds.
      - name: Pack and build Lynx ${{ matrix.lane }} compatibility graph
        run: >-
          node packages/rspeedy-plugin-octane/scripts/compatibility-smoke.mjs
          --lane "${{ matrix.lane }}"

      - name: Verify the repository lockfile stayed immutable
        run: git diff --exit-code -- pnpm-lock.yaml

  package:
    name: validate publishable packages
    needs: release_change
    if: ${{ always() && !cancelled() && github.event.action != 'closed' && (github.event_name != 'pull_request' || github.event.pull_request.draft == false) && needs.release_change.outputs.is_release_pr != 'true' && needs.release_change.outputs.full_ci != 'false' }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6

      - name: Install pnpm
        uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6

      - name: Setup Node.js
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: 24
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # The privileged publish workflow uses this network-free contract to
      # classify registry state before Changesets is allowed to publish.
      - name: Test npm release reconciliation
        run: pnpm release:preflight:test

      # Pack the live, discovered package set instead of a hand-maintained
      # allowlist. The validator checks the post-pnpm manifest and tar contents,
      # including exports/bin/types and workspace/catalog protocol replacement,
      # then installs packed core + binding tarballs into an isolated consumer
      # to prove singleton resolution and client/server raw-source compilation.
      - name: Pack and validate every publishable package
        run: pnpm packages:pack:check

  # This stable marker lets consecutive lightweight commits inherit the last
  # full suite transitively. A lightweight run reaches it only after the scope
  # classifier verifies that its base already had equivalent provenance.
  provenance:
    name: CI provenance
    needs: [release_change, test, lint, typecheck, examples, three_compat, lynx_compat, package]
    if: ${{ always() && !cancelled() && github.event.action != 'closed' && (github.event_name != 'pull_request' || github.event.pull_request.draft == false) && needs.release_change.outputs.is_release_pr != 'true' }}
    runs-on: ubuntu-latest

    steps:
      - name: Verify reusable CI coverage
        env:
          FULL_CI: ${{ needs.release_change.outputs.full_ci }}
          RELEASE_CHANGE_RESULT: ${{ needs.release_change.result }}
          TEST_RESULT: ${{ needs.test.result }}
          LINT_RESULT: ${{ needs.lint.result }}
          TYPECHECK_RESULT: ${{ needs.typecheck.result }}
          EXAMPLES_RESULT: ${{ needs.examples.result }}
          THREE_COMPAT_RESULT: ${{ needs.three_compat.result }}
          LYNX_COMPAT_RESULT: ${{ needs.lynx_compat.result }}
          PACKAGE_RESULT: ${{ needs.package.result }}
        run: |
          test "$RELEASE_CHANGE_RESULT" = success
          test "$TEST_RESULT" = success
          test "$LINT_RESULT" = success
          test "$TYPECHECK_RESULT" = success
          test "$EXAMPLES_RESULT" = success

          if [ "$FULL_CI" = false ]; then
            test "$THREE_COMPAT_RESULT" = skipped
            test "$LYNX_COMPAT_RESULT" = skipped
            test "$PACKAGE_RESULT" = skipped
          else
            test "$TYPECHECK_RESULT" = success
            test "$THREE_COMPAT_RESULT" = success
            test "$LYNX_COMPAT_RESULT" = success
            test "$PACKAGE_RESULT" = success
          fi
