{
  "name": "octane-streaming-ssr-bench",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "streaming-ssr bench fixture for octane renderToPipeableStream, authored in .tsrx (sync shell + 10 use(promise) @try boundaries). SSR-only: no client build, no dev server.",
  "dependencies": {
    "octane": "workspace:*"
  },
  "devDependencies": {
    "vite": "catalog:default"
  }
}
