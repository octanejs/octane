{
  "name": "octane-streaming-ssr-benchmarks",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "Node-only streaming SSR bench - shell TTFB, stream-end total, chunk count, bytes, and all-fast throughput for octane renderToPipeableStream vs React Fizz vs Solid renderToStream vs Ripple's stream mode, on a shared product-page fixture (sync shell + 10 staggered Suspense card boundaries).",
  "scripts": {
    "bench": "node run.mjs",
    "bench:smoke": "node run.mjs 3"
  },
  "dependencies": {
    "vite": "catalog:default",
    "react": "catalog:default",
    "react-dom": "catalog:default",
    "solid-js": "catalog:default",
    "@solidjs/web": "catalog:default"
  }
}
