{
  "name": "octane-ssr-http-bench",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "Raw streaming-API benchmark over real HTTP: cold-start TTFB and streaming throughput for octane renderToPipeableStream vs React Fizz, sharing the streaming-ssr fixtures.",
  "scripts": {
    "bench": "node run.mjs",
    "bench:smoke": "node run.mjs 3"
  },
  "dependencies": {
    "vite": "catalog:default",
    "react": "catalog:default",
    "react-dom": "catalog:default"
  }
}
