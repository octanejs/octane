{
  "name": "react-memowall-bench",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "memo-wall bench fixture for React 19 - the reference React column for the two 1000-row memo(Row) walls; same window.__renders probes and window.__mount/__tickA/__tickB/__oneChangeA/__oneChangeB/__ctxA/__ctxB contract as the octane fixtures.",
  "scripts": {
    "dev": "vite --host --port 5208 --strictPort",
    "build": "vite build",
    "preview": "vite preview --port 5208 --strictPort"
  },
  "dependencies": {
    "react": "catalog:default",
    "react-dom": "catalog:default"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "catalog:default",
    "vite": "catalog:default",
    "terser": "catalog:default"
  }
}
