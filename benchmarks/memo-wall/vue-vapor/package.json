{
  "name": "vue-vapor-memowall-bench",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "memo-wall bench fixture for Vue Vapor (3.6 vapor mode) - the fine-grained column for the two 1000-row walls; same window.__renders probes (creation + leaf text-effect re-runs) and window.__mount/__tickA/__oneChangeA/__ctxA/... contract as the other fixtures.",
  "scripts": {
    "dev": "vite --host --port 5223 --strictPort",
    "build": "vite build",
    "preview": "vite preview --port 5223 --strictPort"
  },
  "dependencies": {
    "vue": "catalog:default",
    "@vue/runtime-dom": "catalog:default",
    "@vue/runtime-vapor": "catalog:default"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "catalog:default",
    "vite": "catalog:default"
  }
}
