{
  "name": "octane-jsx-spa-navigation-bench",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "spa-navigation bench fixture for octane authored React-style in .tsx - exposes window.__mount/__navigate/__unmount/__reset for the Playwright harness.",
  "scripts": {
    "dev": "vite --host --port 5311 --strictPort",
    "build": "vite build",
    "preview": "vite preview --port 5311 --strictPort"
  },
  "dependencies": {
    "octane": "workspace:*"
  },
  "devDependencies": {
    "vite": "catalog:default"
  }
}
