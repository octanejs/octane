{
  "name": "react-spa-navigation-bench",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "spa-navigation bench fixture for React 19 - exposes window.__mount/__navigate/__unmount/__reset for the Playwright harness.",
  "scripts": {
    "dev": "vite --host --port 5312 --strictPort",
    "build": "vite build",
    "preview": "vite preview --port 5312 --strictPort"
  },
  "dependencies": {
    "react": "catalog:default",
    "react-dom": "catalog:default"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "catalog:default",
    "vite": "catalog:default"
  }
}
