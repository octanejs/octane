{
  "name": "inferno-chat-stream",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "Streaming-chat fixture for Inferno and the deterministic token-stream ops.",
  "scripts": {
    "dev": "vite --host --port 5323 --strictPort",
    "build": "vite build",
    "preview": "vite preview --port 5323 --strictPort"
  },
  "dependencies": {
    "inferno": "catalog:default"
  },
  "devDependencies": {
    "vite": "catalog:default",
    "terser": "catalog:default"
  }
}
