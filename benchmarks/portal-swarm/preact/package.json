{
  "name": "preact-portal-swarm-bench",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "portal-swarm benchmark fixture for Preact.",
  "scripts": {
    "dev": "vite --host --port 5268 --strictPort",
    "build": "vite build",
    "preview": "vite preview --port 5268 --strictPort"
  },
  "dependencies": {
    "preact": "catalog:default"
  },
  "devDependencies": {
    "@preact/preset-vite": "catalog:default",
    "vite": "catalog:default",
    "terser": "catalog:default"
  }
}
