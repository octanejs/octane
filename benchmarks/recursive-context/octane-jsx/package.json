{
  "name": "octane-jsx-recursive-bench",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "recursive-context bench fixture for octane authored in React-style .tsx (JSX) - the JSX twin of octane-tsrx-recursive-bench; same tree + window.__mount/__update/__unmount/__reset contract.",
  "scripts": {
    "dev": "vite --host --port 5188 --strictPort",
    "build": "vite build",
    "preview": "vite preview --port 5188 --strictPort"
  },
  "dependencies": {
    "octane": "workspace:*"
  },
  "devDependencies": {
    "vite": "catalog:default"
  }
}
