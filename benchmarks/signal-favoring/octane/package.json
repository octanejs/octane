{
  "name": "octane-signal-bench",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "signal-favoring bench fixture for octane - 100-component chain with stateful counters every 10 levels.",
  "scripts": {
    "dev": "vite --host --port 5190 --strictPort",
    "build": "vite build",
    "preview": "vite preview --port 5190 --strictPort"
  },
  "dependencies": {
    "octane-ts": "workspace:*"
  },
  "devDependencies": {
    "vite": "catalog:default"
  }
}
