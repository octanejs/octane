{
  "name": "octane-jsx-signal-bench",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "signal-favoring bench fixture for octane authored in React-style .tsx (JSX) - the JSX twin of octane-tsrx-signal-bench; 100-component chain, same bump contract.",
  "scripts": {
    "dev": "vite --host --port 5194 --strictPort",
    "build": "vite build",
    "preview": "vite preview --port 5194 --strictPort"
  },
  "dependencies": {
    "octane": "workspace:*"
  },
  "devDependencies": {
    "vite": "catalog:default"
  }
}
