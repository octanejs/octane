{
  "name": "ripple-uibench-bench",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "Ripple implementation of the UIbench desktop operation matrix.",
  "scripts": {
    "dev": "vite --host --port 5322 --strictPort",
    "build": "vite build",
    "preview": "vite preview --port 5322 --strictPort"
  },
  "dependencies": {
    "ripple": "catalog:default"
  },
  "devDependencies": {
    "@ripple-ts/vite-plugin": "catalog:default",
    "terser": "catalog:default",
    "vite": "catalog:default"
  }
}
