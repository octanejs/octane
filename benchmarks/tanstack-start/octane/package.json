{
  "name": "tanstack-start-bench-octane",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Octane flavor of the Start benchmark app — the repository's @octanejs/tanstack-start package on the native stream-injection path.",
  "scripts": {
    "dev": "vite dev --port 3000",
    "build": "vite build",
    "typecheck": "tsrx-tsc --noEmit -p tsconfig.json",
    "start": "node .output/server/index.mjs"
  },
  "dependencies": {
    "@octanejs/tanstack-router": "workspace:*",
    "@octanejs/tanstack-start": "workspace:*",
    "octane": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:default",
    "nitro": "^3.0.260311-beta",
    "typescript": "catalog:default",
    "vite": "catalog:default"
  }
}
