{
  "name": "octane-jsx-naive-jsbench",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "js-framework-benchmark fixture for octane authored in NAIVE React-style .tsx (cross-module Row component, rows.map + ternaries, {...spread} on the row, member-callee handlers, inline style objects) - the de-opt authoring-cliff twin of octane-jsx-jsbench; same DOM/button contract.",
  "scripts": {
    "dev": "vite --host --port 5214 --strictPort",
    "build": "vite build",
    "preview": "vite preview --port 5214 --strictPort"
  },
  "dependencies": {
    "octane": "workspace:*"
  },
  "devDependencies": {
    "vite": "catalog:default",
    "terser": "catalog:default"
  }
}
