{
  "name": "inferno-jsbench",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "Native Inferno implementation of the keyed js-framework benchmark matrix.",
  "scripts": {
    "dev": "vite --host --port 5320 --strictPort",
    "build": "vite build",
    "preview": "vite preview --port 5320 --strictPort"
  },
  "dependencies": {
    "inferno": "catalog:default"
  },
  "devDependencies": {
    "vite": "catalog:default",
    "terser": "catalog:default"
  }
}
