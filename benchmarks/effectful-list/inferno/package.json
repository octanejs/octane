{
  "name": "inferno-effectful-list-bench",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "Native Inferno effectful-list fixture with 1000 lifecycle- and ref-observed rows.",
  "scripts": {
    "dev": "vite --host --port 5330 --strictPort",
    "build": "vite build",
    "preview": "vite preview --port 5330 --strictPort"
  },
  "dependencies": {
    "inferno": "catalog:default"
  },
  "devDependencies": {
    "vite": "catalog:default",
    "terser": "catalog:default"
  }
}
