{
  "name": "preact-news-bench",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "News-site SSR and hydration benchmark fixture for Preact.",
  "scripts": {
    "dev": "vite --host --port 5270 --strictPort"
  },
  "dependencies": {
    "preact": "catalog:default",
    "preact-render-to-string": "catalog:default"
  },
  "devDependencies": {
    "@preact/preset-vite": "catalog:default",
    "vite": "catalog:default"
  }
}
