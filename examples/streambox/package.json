{
  "name": "streambox-example",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "A deterministic video platform exercising Octane native media events, persistent DOM state, and virtualized comments.",
  "scripts": {
    "dev": "vite --host --port 5223 --strictPort",
    "build": "vite build",
    "preview": "vite preview --host --port 5223 --strictPort",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test:e2e": "pnpm build && OCTANE_EXAMPLE_PREVIEW=1 playwright test --config e2e/playwright.config.ts",
    "test:e2e:dev": "playwright test --config e2e/playwright.config.ts"
  },
  "dependencies": {
    "@octanejs/tanstack-virtual": "workspace:*",
    "octane": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "catalog:default",
    "@types/node": "catalog:default",
    "fix-webm-duration": "1.0.6",
    "typescript": "catalog:default",
    "vite": "catalog:default"
  }
}
