// `@octanejs/jotai/utils` — mirror of upstream jotai's utils.ts: the vanilla
// utils (re-exported verbatim from jotai) plus the ported binding-side utils.
export * from './vanilla/utils';
export * from './react/utils';
