diff --git a/tests/upstream/react/basic.test.tsx b/tests/upstream/react/basic.test.tsx
--- a/tests/upstream/react/basic.test.tsx
+++ b/tests/upstream/react/basic.test.tsx
@@ -111,18 +111,22 @@ it('uses a write-only derived atom', () => {
 
   const Counter = () => {
     const [count] = useAtom(countAtom)
+    // OCTANE DIVERGENCE[component-commit-observation]: observe the owning component commit, outside the child template scope.
+    const commitCount = useCommitCount()
     return (
       <div>
-        commits: {useCommitCount()}, count: {count}
+        commits: {commitCount}, count: {count}
       </div>
     )
   }
 
   const Control = () => {
     const [, increment] = useAtom(incrementCountAtom)
+    // OCTANE DIVERGENCE[component-commit-observation]: observe the owning component commit, outside the child template scope.
+    const commitCount = useCommitCount()
     return (
       <>
-        <div>button commits: {useCommitCount()}</div>
+        <div>button commits: {commitCount}</div>
         <button onClick={() => increment()}>button</button>
       </>
     )
@@ -151,10 +155,12 @@ it('only re-renders if value has changed', () => {
   type Props = { countAtom: typeof count1Atom; name: string }
   const Counter = ({ countAtom, name }: Props) => {
     const [count, setCount] = useAtom(countAtom)
+    // OCTANE DIVERGENCE[component-commit-observation]: observe the owning component commit, outside the child template scope.
+    const commitCount = useCommitCount()
     return (
       <>
         <div>
-          commits: {useCommitCount()}, {name}: {count}
+          commits: {commitCount}, {name}: {count}
         </div>
         <button onClick={() => setCount((c) => c + 1)}>button-{name}</button>
       </>
@@ -163,10 +169,12 @@ it('only re-renders if value has changed', () => {
 
   const Product = () => {
     const [product] = useAtom(productAtom)
+    // OCTANE DIVERGENCE[component-commit-observation]: observe the owning component commit, outside the child template scope.
+    const commitCount = useCommitCount()
     return (
       <>
         <div data-testid="product">
-          commits: {useCommitCount()}, product: {product}
+          commits: {commitCount}, product: {product}
         </div>
       </>
     )
@@ -242,10 +250,12 @@ it('works with async get', async () => {
   const Counter = () => {
     const [count, setCount] = useAtom(countAtom)
     const [delayedCount] = useAtom(asyncCountAtom)
+    // OCTANE DIVERGENCE[component-commit-observation]: observe the owning component commit, outside the child template scope.
+    const commitCount = useCommitCount()
     return (
       <>
         <div>
-          commits: {useCommitCount()}, count: {count}, delayedCount:{' '}
+          commits: {commitCount}, count: {count}, delayedCount:{' '}
           {delayedCount}
         </div>
         <button onClick={() => setCount((c) => c + 1)}>button</button>
@@ -340,10 +350,12 @@ it('uses atoms with tree dependencies', async () => {
   const Counter = () => {
     const [count] = useAtom(leftAtom)
     const [, setCount] = useAtom(rightAtom)
+    // OCTANE DIVERGENCE[component-commit-observation]: observe the owning component commit, outside the child template scope.
+    const commitCount = useCommitCount()
     return (
       <>
         <div>
-          commits: {useCommitCount()}, count: {count}
+          commits: {commitCount}, count: {count}
         </div>
         <button onClick={() => setCount((c) => c + 1)}>button</button>
       </>
@@ -415,10 +427,12 @@ it('uses an async write-only atom', async () => {
   const Counter = () => {
     const [count] = useAtom(countAtom)
     const [, setCount] = useAtom(asyncCountAtom)
+    // OCTANE DIVERGENCE[component-commit-observation]: observe the owning component commit, outside the child template scope.
+    const commitCount = useCommitCount()
     return (
       <>
         <div>
-          commits: {useCommitCount()}, count: {count}
+          commits: {commitCount}, count: {count}
         </div>
         <button onClick={() => setCount((c) => c + 1)}>button</button>
       </>
@@ -537,10 +551,12 @@ it('only invoke read function on use atom', () => {
   const Counter = () => {
     const [count, setCount] = useAtom(countAtom)
     const [doubledCount] = useAtom(doubledCountAtom)
+    // OCTANE DIVERGENCE[component-commit-observation]: observe the owning component commit, outside the child template scope.
+    const commitCount = useCommitCount()
     return (
       <>
         <div>
-          commits: {useCommitCount()}, count: {count}, readCount: {readCount},
+          commits: {commitCount}, count: {count}, readCount: {readCount},
           doubled: {doubledCount}
         </div>
         <button onClick={() => setCount((c) => c + 1)}>button</button>
@@ -734,9 +750,11 @@ it('changes atom from parent (#273, #275)', () => {
   const Item = ({ id }: { id: string }) => {
     const a = useMemo(() => (id === 'a' ? atomA : atomB), [id])
     const [atomValue] = useAtom(a)
+    // OCTANE DIVERGENCE[component-commit-observation]: observe the owning component commit, outside the child template scope.
+    const commitCount = useCommitCount()
     return (
       <div>
-        commits: {useCommitCount()}, id: {atomValue.id}
+        commits: {commitCount}, id: {atomValue.id}
       </div>
     )
   }
@@ -886,9 +904,11 @@ it('sync re-renders with useState re-renders (#827)', () => {
       atoms[currentAtomIndex] as (typeof atoms)[number],
     )
 
+    // OCTANE DIVERGENCE[component-commit-observation]: observe the owning component commit, outside the child template scope.
+    const commitCount = useCommitCount()
     return (
       <>
-        <span>commits: {useCommitCount()}</span>
+        <span>commits: {commitCount}</span>
         <h1>{atomValue}</h1>
         <button onClick={rotateAtoms}>rotate</button>
       </>
