diff --git a/tests/upstream/react/async.test.tsx b/tests/upstream/react/async.test.tsx
--- a/tests/upstream/react/async.test.tsx
+++ b/tests/upstream/react/async.test.tsx
@@ -636,10 +636,12 @@ it('a derived atom from a newly created async atom (#351)', async () => {
   const Counter = () => {
     const [, setCount] = useAtom(countAtom)
     const [derived] = useAtom(derivedAtom)
+    // OCTANE DIVERGENCE[component-commit-observation]: observe the owning component commit, outside the child template scope.
+    const commitCount = useCommitCount()
     return (
       <>
         <div>
-          derived: {derived}, commits: {useCommitCount()}
+          derived: {derived}, commits: {commitCount}
         </div>
         <button onClick={() => setCount((c) => c + 1)}>button</button>
       </>
