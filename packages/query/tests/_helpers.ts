// Reuse octane's test helpers (same octane runtime instance resolves for both
// the binding and the fixtures).
export { mount, nextPaint, flushEffects, createLog } from '../../octane/tests/_helpers';
