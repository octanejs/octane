// A legal module binding must not capture the compiler's anonymous-default LOC helper.
const Object = null;

export default () => [
	<div key="default-arrow" id="default-arrow-root">default arrow root</div>,
]
