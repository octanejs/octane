{
  "$schema": "./styling-failure-ledger.schema.json",
  "schemaVersion": 1,
  "entries": [
    {
      "id": "SFL-001",
      "fixture": "packages/octane/tests/_fixtures/styling-failures/stale-scoped-styles.tsrx",
      "failure": "A refactoring agent renamed a markup class but left the old rule in the component's scoped <style> block; the compiler pruned the dead selector into a `/* (unused) */` comment with no diagnostic, so the stale style shipped silently.",
      "expectedCode": "octane-css-unused-selector",
      "disposition": "prose:website/src/content/docs/styling.mdx",
      "notes": "Re-triage to diagnostic:octane-css-unused-selector once the unused-selector check ships, or to an eval task covering stale-refactor repair."
    },
    {
      "id": "SFL-002",
      "fixture": "packages/octane/tests/_fixtures/styling-failures/shorthand-longhand-merge-clash.tsrx",
      "failure": "An agent wrote the `border` shorthand in a theme applied over a base theme that set `border-top-color`; the merged classes put the shorthand after the longhand in the emitted sheet order, silently clobbering the longhand at runtime.",
      "expectedCode": "octane-css-shorthand-longhand-clash",
      "disposition": "prose:website/src/content/docs/styling.mdx",
      "notes": "Re-triage to diagnostic:octane-css-shorthand-longhand-clash once the shorthand/longhand clash check ships, or to an eval task covering merge-clash repair."
    }
  ]
}
