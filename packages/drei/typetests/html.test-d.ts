import { Html, defaultCalculatePosition, type HtmlProps } from '../src/index.js';

const props: HtmlProps = {
	center: true,
	transform: true,
	sprite: true,
	occlude: 'blending',
	className: 'label',
	wrapperClass: 'wrapper',
	pointerEvents: 'auto',
};

void Html;
void props;
void defaultCalculatePosition;
