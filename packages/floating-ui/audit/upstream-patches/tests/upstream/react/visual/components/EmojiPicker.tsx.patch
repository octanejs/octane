diff --git a/tests/upstream/react/visual/components/EmojiPicker.tsx b/tests/upstream/react/visual/components/EmojiPicker.tsx
--- a/tests/upstream/react/visual/components/EmojiPicker.tsx
+++ b/tests/upstream/react/visual/components/EmojiPicker.tsx
@@ -16,7 +16,7 @@ import {
   useRole,
 } from '@octanejs/floating-ui';
 import c from 'clsx';
-import {forwardRef, useEffect, useRef, useState} from 'octane';
+import {useEffect, useRef, useState} from 'octane';
 
 import {Button} from '../lib/Button';
 
@@ -66,10 +66,15 @@ type OptionProps = React.HTMLAttributes<HTMLButtonElement> & {
   children: React.ReactNode;
 };
 
-const Option = forwardRef<HTMLButtonElement, OptionProps>(function Option(
-  {name, active, selected, children, ...props},
+// OCTANE DIVERGENCE[floating-ui-ref-prop]: Octane passes refs as ordinary props.
+function Option({
+  name,
+  active,
+  selected,
+  children,
   ref,
-) {
+  ...props
+}: OptionProps & {ref?: unknown}) {
   const id = useId();
   return (
     <button
@@ -94,7 +99,7 @@ const Option = forwardRef<HTMLButtonElement, OptionProps>(function Option(
       {children}
     </button>
   );
-});
+}
 
 export const Main = () => {
   const [open, setOpen] = useState(false);
