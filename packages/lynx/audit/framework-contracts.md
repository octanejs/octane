# Lynx framework contract audit

Recorded against the exact package and SDK set in `toolchain.json` on
2026-07-18. “Typed” means the selected published declaration package exposes
the surface; it does not by itself prove identical behavior on every native
engine. The device gates in `phase-0-evidence.json` remain authoritative.

## Classification

- **Public documented** — described as a framework/application API in the Lynx
  documentation and present in a published package.
- **Public typed** — present in a published Lynx declaration, but the exact
  framework-author lifecycle behavior still needs real-engine evidence.
- **Published low-level** — exported by a published tool package and exercised
  by upstream source/tests, but sensitive to the pinned 0.x toolchain.
- **Injected/private** — installed through ReactLynx-owned or engine injection
  objects and absent from the selected public declaration packages.
- **Unresolved** — the spike has no stable public contract or native execution
  evidence sufficient for a port.

## Contract matrix

| Required surface | Classification | Evidence | Milestone 0 decision |
| --- | --- | --- | --- |
| Create, mutate, inspect parentage, insert, remove, and flush PAPI nodes | Public documented and typed | [Element PAPI for framework authors](https://lynxjs.org/api/engine/element-api); [`__GetParent`](https://lynxjs.org/api/engine/element-api/__getparent) returns the current parent or `undefined`, and [`__ElementIsEqual`](https://lynxjs.org/api/engine/element-api/__elementisequal) compares opaque element references. `@lynx-js/type-element-api/types/element-api.d.ts` declares these alongside `__CreatePage`, `__CreateElement`, `__CreateRawText`, `__AppendElement`, `__SetAttribute`, `__AddDataset`, `__RemoveElement`, and `__FlushElementTree`. | Suitable for the Octane-owned main-thread receiver, subject to native execution. Parent inspection plus opaque-reference equality makes terminal root removal idempotent when a PAPI operation's mutation outcome is ambiguous. |
| Apply classes, inline styles, CSS IDs, and complete datasets | Public documented and typed; testing-environment deletion behavior is incomplete | The pinned Element PAPI installs `__SetClasses`, `__SetInlineStyles`, `__SetCSSId`, and `__SetDataset`. Octane sends complete normalized values through those operations rather than lowering them to unrelated attributes. In `@lynx-js/testing-environment@0.3.0`, `__SetDataset` uses `Object.assign`, so that model cannot prove removal of a key omitted from a later complete bag. | Suitable for the Milestone 3 host boundary. Keep real-engine replacement/removal evidence open, especially dataset-key deletion; the JavaScript testing environment proves routing and normalization, not that deletion contract. |
| Create and recycle native list cells | Public typed; JavaScript model exercised; native allocation unresolved | The selected Element PAPI exposes `__CreateList`, `__UpdateListComponents`, and `__UpdateListCallbacks`. The official testing environment exposes list-item enter/leave operations that call the registered `componentAtIndex` and `enqueueComponent` callbacks. Octane keeps logical items in the Lynx driver and creates a physical tree only when that callback requests an index. | Suitable for the private Milestone 4 source boundary. A 1,000-item JavaScript test proves demand materialization, cell identity reuse, reorder, attach/detach ordering, and inert callbacks after teardown. A deterministic fake-PAPI benchmark records 12 physical cells for a 12-cell window, 988 reuses, zero cells after teardown, and a 0.012 eager-allocation ratio. Neither lane proves Android/iOS native node counts, memory, scroll behavior, layout, or callback timing. Nested lists are rejected initially. Boolean `defer` is forwarded as metadata but all initial cells are callback-demanded, and `defer.unmountRecycled` is unsupported. |
| Notify a renderer-neutral root that a host became physically attached or detached | Octane-owned optional universal capability | Lynx recycling makes logical membership differ from physical native presence. The optional `UniversalHostDriver.attachments` capability reports host IDs only; the universal core orders detach parent-first and attach child-first and applies ordinary ref semantics. Drivers without the capability retain their existing always-attached behavior. | Generalize physical attachment, not list operations. The object-driver contract and Lynx transport tests prove ref behavior; native list/device evidence remains open. |
| Register a native event token | Public documented and typed | [`__AddEvent`](https://lynxjs.org/api/engine/element-api/__addevent) accepts a string listener token; the declaration is in `@lynx-js/type-element-api/types/element-api.d.ts`. | Token installation is usable. This does not establish the background receiver contract below. |
| Register a main-thread worklet event | Published/tested protocol shape; framework-neutral native contract unresolved | The pinned ReactLynx implementation and `@lynx-js/testing-environment@0.3.0` pass `{ type: 'worklet', value: { _wkltId, _c? } }` to Element PAPI and execute it through `runWorklet`. Octane emits that interoperable data envelope but owns the directive transform, stable IDs, clone-safe captures, registry revisions, activation tokens, and host-removal plus active/retained definition-revision invalidation; full module-removal/HMR invalidation remains Milestone 8. It imports no ReactLynx component, Preact, transform, or worklet runtime. | Suitable for private Milestone 7 source and JavaScript-host tests only. Keep the native gate open until the envelope is documented for framework authors and tap, scroll, and gesture handlers demonstrably execute on the main thread without a background callback or round trip on Explorer, Android, and iOS. |
| Update a main-thread ref with an Element PAPI node | Injected/runtime-owned and unresolved | The pinned worklet path represents refs as `{ _wvid }` and updates them through `lynxWorkletImpl._refImpl.updateWorkletRef`; the JavaScript testing environment models that hook. The selected public framework declarations do not provide a stable lifecycle contract for this ref runtime. Octane transports a clone-safe initial value separately, strips it from the PAPI descriptor, and keeps the live Element PAPI object on the main thread. Host detach sets `current` to `null`; a mounted hook owner preserves the same cell across calls and host remounts. The registry collects the cell after host, hook-owner, and activation retains all release, while successful root disposal or controller close forcibly clears every remaining cell. | **Stop gate for a native main-thread-ref claim.** JavaScript-host attachment, adoption, state-persistence, and cleanup tests do not prove native node identity or update timing. Confirm or upstream the framework-neutral hook and exercise it on Explorer, Android, and iOS. |
| Send a background commit and return an ACK | Public primitives, Octane-owned protocol | `ContextProxy` in `@lynx-js/types/types/common/lynx.d.ts` exposes `postMessage`, `dispatchEvent`, `addEventListener`, and `removeEventListener`; `CommonLynx` exposes `getCoreContext()` and `getJSContext()`. | The probe may define versioned commit/ACK semantics over these primitives. The background waits for a main-ready handshake before mounting, and ACK is sent only after validation, PAPI application, and the single flush. Terminal destroy is an idempotent request outside commit sequencing, with responses correlated by request ID rather than commit version, so cleanup does not depend on agreeing which faulted commit applied. The receiver records an intended page-root append before entering PAPI so terminal cleanup can also remove a root when append mutates and then throws. |
| Invoke a main-thread worklet or retained background function and settle a result | Public transport primitives, Octane-owned call protocol | Milestone 7 adds root/birth/version-correlated `call-main` and `call-background` messages over `ContextProxy`, plus result, serialized error, and cancellation messages. Calls made before adoption queue behind the ownership handshake; arguments, captures, and results cross a strict clone-safe boundary; close, root replacement, cancellation, duplicate/late completion, and active/retained definition-revision changes make old work inert. Raw never-activated descriptors and complete module-removal/HMR invalidation remain Milestone 8 work. | Suitable for private source and JavaScript-host testing. Exactly-once async value/error/cancel observations do not prove native scheduling, cancellation timing, or continuous-event latency. Preserve the stale-root and bounded pre-adoption-queue checks in every native lane. |
| Receive a string-token native event in the background runtime | Injected/private and unresolved | The published test environment calls `globalThis.lynxCoreInject.tt.publishEvent`; ReactLynx installs `publishEvent` and `publicComponentEvent` in `packages/react/runtime/src/element-template/native/index.ts`. Neither `lynxCoreInject.tt` receiver appears in `@lynx-js/types@4.0.0` or `@lynx-js/type-element-api@0.0.8`. Octane's `dispatchNativeEvent*` exports provide a source/test bridge with the intended data-only payload, priority, propagation, ACK, and generation rules; they are not a native callback receiver. | **Stop gate.** Upstream or obtain a documented framework-neutral receiver before enabling production native events. Passing bridge tests establish Octane's behavior after delivery, not the missing framework-neutral delivery hook. |
| Select, query, measure, invoke, and set native props from the background runtime | Public typed; real-engine behavior not yet proven | `@lynx-js/types@4.0.0` declares background `lynx.createSelectorQuery()`, `select()`, `invoke()`, `fields()`, `path()`, and `setNativeProps()`. Octane uses generation-scoped `[octane-ref="…"]` selectors and invalidates pending handles when their host generation is removed. The pinned JavaScript testing environment exercises selector lookup and `setNativeProps`, but does not model every invoke, fields, path, measurement, and layout result. | Suitable for the Milestone 3 typed/query boundary with generation and ACK guards. Keep Explorer, Android, and iOS execution evidence open for selector uniqueness, layout/measurement options, method invocation, and stale async completion. |
| Main-thread render/update/global-props lifecycle messages | Public typed; engine behavior not yet proven | `LynxMessageEvent` in `@lynx-js/types/types/common/events.d.ts` includes `__RenderPage`, `__UpdatePage`, and `__UpdateGlobalProps`. | Implement only after the 3.9.0 native matrix confirms which context receives each event and its ordering. |
| Initial data and background global props | Public typed bootstrap/events plus pinned framework current-data contract; source integration implemented; device ordering unresolved | `@lynx-js/types@4.0.0` declares background `lynx.__presetData`, `lynx.__globalProps`, and `getJSModule()`. The pinned ReactLynx runtime maintains `lynx.__initData` before publishing `onDataChanged`, including replacing it before RESET delivery. Octane's background-only platform subpath clones the current snapshot when present, falls back to public preset data, and subscribes to the public `GlobalEventEmitter` names `onDataChanged` and `onGlobalPropsChanged`; source tests cover rerender, reset key removal, an update before layout subscription, callback snapshots, replacement, and cleanup. | Suitable for the private Milestone 4 hook boundary. Octane does not yet install the framework/native update receiver that owns the current snapshot. The JavaScript tests do not establish which native context publishes each event, update ordering, payload completeness, or reload/destroy interaction on Android and iOS. |
| Background global events, Native Modules, reload requests, and error reports | Public application APIs; framework lifecycle receivers remain unresolved | The selected background types expose `getJSModule('GlobalEventEmitter')`, the `NativeModules` global, `lynx.reload()`, and `lynx.reportError()`. Octane exposes typed/augmentable access and source-tested forwarding. Main-thread compiler specialization rejects statically visible `NativeModules` and `@octanejs/lynx/platform` imports. | Suitable for application-owned background calls and source diagnostics. `reload()` is request forwarding only; it does not replace ReactLynx's private framework reload receiver. The illustrative Android/iOS modules and custom elements are not native execution evidence. |
| Page destroy on the native context | Public typed; engine behavior not yet proven | `LynxMessageEvent` includes `__DestroyLifetime`, and `CommonLynx.getNative()` returns a typed `ContextProxy`. Current ReactLynx main-thread code also listens through `getNative().addEventListener('__DestroyLifetime', ...)`. | Candidate framework-neutral teardown signal. It still requires Explorer, Android, and iOS evidence before acceptance. |
| Background destroy callback | Injected/private | ReactLynx assigns `lynxCoreInject.tt.callDestroyLifetimeFun`; it is absent from `@lynx-js/types@4.0.0`. | **Stop gate** unless the typed native destroy event is confirmed to reach and safely teardown the background runtime on all required engines. |
| Background reload callback | Injected/private | ReactLynx assigns `lynxCoreInject.tt.onAppReload`; it is absent from `@lynx-js/types@4.0.0`. `lynx.reload()` is a public request API, not a framework reload receiver. | **Stop gate.** A public reload/update lifecycle receiver must be confirmed or upstreamed. |
| Background card-data/global-props injection callbacks | Injected/private | ReactLynx assigns `updateCardData` and `updateGlobalProps` on `lynxCoreInject.tt`; these receiver hooks are absent from the selected public types. | Prefer the typed context events if native evidence proves them sufficient; otherwise upstream a neutral receiver. |
| Thread-specific bundle entries | Probe-owned build contract; engine execution unresolved | The Phase 0 plugin maps separate main/background source entries to the template chunks, and build verification confirms that the decoded programs use only their respective `getJSContext()` / `getCoreContext()` integration. Neither program branches on `__MAIN_THREAD__` or `__BACKGROUND__`. | The probe no longer depends on undocumented thread globals. Confirm both explicit entries execute on every required real engine before accepting the bootstrap contract. |
| Main-thread bootstrap via top-level execution or `globalThis.renderPage` | Unresolved | Official imperative examples use both top-level PAPI construction and an assigned `renderPage` callback; the selected public type packages do not define a framework bootstrap interface. | Real-engine spike must identify the supported bootstrap and lifecycle ordering before Milestone 1. |
| Mark a compiled asset as main-thread code | Published low-level | `LynxTemplatePlugin` consumes asset info containing `'lynx:main-thread': true`; upstream template-plugin tests and ReactLynx’s entry plugin use this convention. | Accept only for the exact pinned template/Rspack set; protect it with a production-build test. |
| Assemble and encode a production `.lynx.bundle` | Published low-level | `LynxTemplatePlugin`, `RuntimeWrapperWebpackPlugin`, and `LynxEncodePlugin` are published packages. The probe marks main-thread output, wraps background JavaScript, and encodes with `inlineScripts: true`. | The resulting bundle is a valid build artifact, but decoding it is not engine execution. |

## Probe-specific conclusions

The published JavaScript testing environment is valuable for protocol and PAPI
tests, but it deliberately models some engine behavior. In particular,
`__FlushElementTree` is a no-op there, `__SetDataset` merges rather than proving
whole-bag key deletion, and string event delivery resolves through the injected
`lynxCoreInject.tt.publishEvent`. Its selector/query model also covers only part
of the typed `NodesRef` surface. The probe’s adapter-level flush counter and
explicit test injection therefore validate Octane’s observation boundary
without turning those modeled behaviors into public engine contracts.

The current probe should remain React-free. `dsl: "react_nodiff"` is template
encoder metadata used for a framework-owned PAPI tree; it is not permission to
install ReactLynx or copy `pluginReactLynx()` wholesale.

The pinned Web-core source calls `globalThis.renderPage?.(processedData)` after
loading the main-thread root and then flushes the element tree. The probe and
imperative control both install `renderPage`, `updatePage`, and
`updateGlobalProps`; nevertheless, fresh Chromium runs first fail before
rendering with the same `MutationObserver.observe` non-Node target error. The
transported path additionally reports that Web `postMessage` is unimplemented.
Because the direct-PAPI control shares the first bootstrap failure, Phase 0
records this as an unresolved Web/toolchain gate rather than attributing that
failure to Octane's transport.

## Gate decision

Milestone 0 is **blocked from exit**, not failed. PAPI mutation and context
transport have sufficient public surface for the spike, while background native
event delivery and reload/background teardown do not yet have a confirmed
framework-neutral contract. Per the migration plan, do not begin the production
port until those hooks are documented or upstreamed and the exact 3.9.0 bundle
passes Explorer, Android, and iOS execution gates.
