diff --git a/tests/upstream/useFieldArray/dirtyFields.test.tsx b/tests/upstream/useFieldArray/dirtyFields.test.tsx
--- a/tests/upstream/useFieldArray/dirtyFields.test.tsx
+++ b/tests/upstream/useFieldArray/dirtyFields.test.tsx
@@ -6,7 +6,10 @@ import { useForm } from '../../../src/useForm';
 
 let i = 0;
 
-vi.mock('../../../src/logic/generateId', () => () => String(i++));
+// octane: vitest module mock — path resolved from tests/upstream/useFieldArray/, factory returns { default }
+vi.mock('../../../src/logic/generateId', () => ({
+  default: () => String(i++),
+}));
 
 describe('useFieldArray dirtyFields isolation', () => {
   beforeEach(() => {
