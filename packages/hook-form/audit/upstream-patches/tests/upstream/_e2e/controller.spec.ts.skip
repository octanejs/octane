These three browser registrations mount React MUI 5 components and assert MUI portal DOM. There is no Octane MUI binding in this repository. They remain executable in the complete pristine React browser lane and visible as unsupported integrations in the case crosswalk. Native Controller validation and event contracts run in the adapted source, differential, and real-browser lanes. This campaign updates existing bindings; it does not introduce a MUI port.
