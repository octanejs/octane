diff --git a/tests/upstream/useForm.server.test.tsx b/tests/upstream/useForm.server.test.tsx
--- a/tests/upstream/useForm.server.test.tsx
+++ b/tests/upstream/useForm.server.test.tsx
@@ -18,7 +18,7 @@ describe('useForm with SSR', () => {
 
     const spy = vi.spyOn(console, 'error');
 
-    expect(renderToString(<Component />)).toEqual(
+    expect(renderToStaticMarkup(Component).html).toEqual(
       '<div><input name="test"/></div>',
     );
 
@@ -46,7 +46,7 @@ describe('useForm with SSR', () => {
       );
     };
 
-    expect(renderToString(<App />)).toEqual(
+    expect(renderToStaticMarkup(App).html).toEqual(
       '<div><input name="test"/><span role="alert">test error</span></div>',
     );
   });
@@ -72,7 +72,9 @@ describe('useForm with SSR', () => {
       );
     };
 
-    expect(renderToString(<App />)).toEqual('<div><input name="test"/></div>');
+    expect(renderToStaticMarkup(App).html).toEqual(
+      '<div><input name="test"/></div>',
+    );
   });
 
   it('should pass down constrained API for server side rendering', () => {
@@ -98,7 +100,9 @@ describe('useForm with SSR', () => {
       );
     };
 
-    expect(renderToString(<App />)).toEqual('<div><input name="test"/></div>');
+    expect(renderToStaticMarkup(App).html).toEqual(
+      '<div><input name="test"/></div>',
+    );
   });
 
   it('should support progress enhancement for form', () => {
@@ -124,7 +128,7 @@ describe('useForm with SSR', () => {
       );
     };
 
-    expect(renderToString(<App />)).toEqual(
+    expect(renderToStaticMarkup(App).html).toEqual(
       '<div><input required="" min="2" max="2" minLength="2" maxLength="2" name="test"/></div>',
     );
   });
