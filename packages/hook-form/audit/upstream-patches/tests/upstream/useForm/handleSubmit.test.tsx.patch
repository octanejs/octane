diff --git a/tests/upstream/useForm/handleSubmit.test.tsx b/tests/upstream/useForm/handleSubmit.test.tsx
--- a/tests/upstream/useForm/handleSubmit.test.tsx
+++ b/tests/upstream/useForm/handleSubmit.test.tsx
@@ -23,7 +23,7 @@ describe('handleSubmit', () => {
       await result.current.handleSubmit(callback)({
         preventDefault: noop,
         persist: noop,
-      } as React.SyntheticEvent);
+      });
     });
     expect(callback).toHaveBeenCalled();
   });
@@ -115,7 +115,7 @@ describe('handleSubmit', () => {
       })({
         preventDefault: noop,
         persist: noop,
-      } as React.SyntheticEvent);
+      });
     });
   });
 
@@ -143,7 +143,7 @@ describe('handleSubmit', () => {
       })({
         preventDefault: noop,
         persist: noop,
-      } as React.SyntheticEvent);
+      });
     });
   });
 
@@ -166,7 +166,7 @@ describe('handleSubmit', () => {
       })({
         preventDefault: noop,
         persist: noop,
-      } as React.SyntheticEvent);
+      });
     });
 
     await act(async () => {
@@ -175,7 +175,7 @@ describe('handleSubmit', () => {
       })({
         preventDefault: noop,
         persist: noop,
-      } as React.SyntheticEvent);
+      });
     });
   });
 
@@ -190,7 +190,7 @@ describe('handleSubmit', () => {
       await result.current.handleSubmit(callback)({
         preventDefault: noop,
         persist: noop,
-      } as React.SyntheticEvent);
+      });
     });
     expect(callback).not.toHaveBeenCalled();
   });
@@ -212,7 +212,7 @@ describe('handleSubmit', () => {
       await result.current.handleSubmit(callback)({
         preventDefault: noop,
         persist: noop,
-      } as React.SyntheticEvent);
+      });
     });
 
     expect(callback).not.toHaveBeenCalled();
@@ -237,7 +237,7 @@ describe('handleSubmit', () => {
       await result.current.handleSubmit(callback)({
         preventDefault: noop,
         persist: noop,
-      } as React.SyntheticEvent);
+      });
     });
 
     expect(callback).not.toHaveBeenCalled();
@@ -294,7 +294,7 @@ describe('handleSubmit', () => {
       await result.current.handleSubmit(noop)({
         preventDefault: noop,
         persist: noop,
-      } as React.SyntheticEvent);
+      });
     });
 
     await act(async () => {
@@ -327,7 +327,7 @@ describe('handleSubmit', () => {
       })({
         preventDefault: noop,
         persist: noop,
-      } as React.SyntheticEvent),
+      }),
     );
   });
 
@@ -361,7 +361,7 @@ describe('handleSubmit', () => {
       await result.current.handleSubmit(callback)({
         preventDefault: noop,
         persist: noop,
-      } as React.SyntheticEvent);
+      });
     });
 
     expect(callback).not.toHaveBeenCalled();
@@ -372,7 +372,7 @@ describe('handleSubmit', () => {
       await result.current.handleSubmit(callback)({
         preventDefault: noop,
         persist: noop,
-      } as React.SyntheticEvent);
+      });
     });
 
     expect(callback).toHaveBeenCalled();
@@ -448,7 +448,7 @@ describe('handleSubmit', () => {
         await result.current.handleSubmit(callback)({
           preventDefault: noop,
           persist: noop,
-        } as React.SyntheticEvent);
+        });
       });
       expect(callback).toHaveBeenCalled();
     });
@@ -476,7 +476,7 @@ describe('handleSubmit', () => {
         await result.current.handleSubmit(callback)({
           preventDefault: noop,
           persist: noop,
-        } as React.SyntheticEvent);
+        });
       });
       expect(callback.mock.calls[0][0]).toEqual({ test: 'test' });
     });
@@ -495,7 +495,7 @@ describe('handleSubmit', () => {
         )({
           preventDefault: noop,
           persist: noop,
-        } as React.SyntheticEvent);
+        });
       });
       expect(onValidCallback).toHaveBeenCalledTimes(1);
       expect(onInvalidCallback).not.toHaveBeenCalledTimes(1);
@@ -518,7 +518,7 @@ describe('handleSubmit', () => {
         )({
           preventDefault: noop,
           persist: noop,
-        } as React.SyntheticEvent);
+        });
       });
 
       expect(onValidCallback).not.toHaveBeenCalledTimes(1);
@@ -540,7 +540,7 @@ describe('handleSubmit', () => {
       })({
         preventDefault: noop,
         persist: noop,
-      } as React.SyntheticEvent);
+      });
     });
 
     await act(async () => {
