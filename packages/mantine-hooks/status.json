{
  "upstream": {
    "package": "@mantine/hooks",
    "version": "9.6.1"
  },
  "surface": "Complete @mantine/hooks 9.6.1 runtime export surface: state, timing, storage, viewport, input, focus, pointer, observer, hotkey, scrolling, collapse, drag, splitter, mask, and utility hooks.",
  "divergences": [
    "Hooks use Octane's compiler-injected hook slots and runtime lifecycle instead of React's dispatcher.",
    "DOM subscriptions receive native browser events.",
    "React is retained only as a source-compatibility type vocabulary for refs, events, actions, and CSS properties; it is not loaded at runtime."
  ],
  "ssr": "Dedicated Node-mode coverage verifies deterministic state-hook output and guarded media-query initial values without a browser. DOM-only effects remain inert during server rendering.",
  "verified": "2026-09-13",
  "provenance": {
    "source": "https://github.com/mantinedev/mantine",
    "license": "MIT",
    "commit": "61049ecd950f6fb9ddc631decfec2edbdffe58e1",
    "verification": "verified"
  },
  "notes": [
    "Complete pinned upstream coverage: 63 files and 545 cases per runtime lane; 223-export public type assertions; client and SSR hydration in Chromium.",
    "Floating-window callbacks and enabled state now use independent native refs; touch cancellation, late-ref subscription, zero-coordinate handling and body-selection restoration are preserved."
  ]
}
