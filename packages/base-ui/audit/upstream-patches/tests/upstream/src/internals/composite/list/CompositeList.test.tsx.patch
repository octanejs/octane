diff --git a/tests/upstream/src/internals/composite/list/CompositeList.test.tsx b/tests/upstream/src/internals/composite/list/CompositeList.test.tsx
--- a/tests/upstream/src/internals/composite/list/CompositeList.test.tsx
+++ b/tests/upstream/src/internals/composite/list/CompositeList.test.tsx
@@ -1,4 +1,8 @@
 /** @jsxImportSource octane */
+import { CompositeListFixture } from '../../../../../fixtures/composite-list-ssr.tsrx';
+import { CompositeListFixture as ServerFixture } from '../../../../../fixtures/composite-list-ssr.tsrx?octane-ssr';
+import { registerServerComponent } from '../../../../../support/renderer';
+registerServerComponent(CompositeListFixture, ServerFixture);
 import { expect, vi, describe, it } from 'vitest';
 import * as React from 'octane';
 import * as ReactDOM from 'octane';
@@ -1107,26 +1111,12 @@ describe('<CompositeList />', () => {
   });
 
   describe('server-side rendering', () => {
-    function Item(props: { label: string }) {
-      const { ref, index } = useCompositeListItem();
-      return <div ref={ref} data-testid={props.label} data-index={index} />;
-    }
-
     it('hydrates a server-rendered list without a mismatch under Strict Mode', () => {
       const elementsRef = {
         current: [] as Array<HTMLElement | null>,
       };
 
-      function App() {
-        return (
-          <CompositeList elementsRef={elementsRef}>
-            <Item label="a" />
-            <Item label="b" />
-          </CompositeList>
-        );
-      }
-
-      const { hydrate } = renderToString(<App />);
+      const { hydrate } = renderToString(<CompositeListFixture elementsRef={elementsRef} />);
       expect(screen.getByTestId('a')).toHaveAttribute('data-index', '-1');
       expect(screen.getByTestId('b')).toHaveAttribute('data-index', '-1');
 
