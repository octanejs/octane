diff --git a/tests/upstream/test/conformanceTests/renderProp.tsx b/tests/upstream/test/conformanceTests/renderProp.tsx
--- a/tests/upstream/test/conformanceTests/renderProp.tsx
+++ b/tests/upstream/test/conformanceTests/renderProp.tsx
@@ -25,8 +25,8 @@ export function testRenderProp(
 
   const nativeButton = Element === 'button';
 
-  const Wrapper = React.forwardRef<any, { children?: React.ReactNode }>(
-    function Wrapper(props, forwardedRef) {
+  const Wrapper = function Wrapper(props: ({ children?: React.OctaneNode }) & { ref?: React.Ref<any> }) {
+  const forwardedRef = props.ref ?? null;
       return wrappingAllowed ? (
         <div data-testid="base-ui-wrapper">
           <Element ref={forwardedRef} {...props} data-testid="wrapped" />
@@ -35,8 +35,7 @@ export function testRenderProp(
         /* @ts-expect-error complex type */
         <Element ref={forwardedRef} {...props} data-testid="wrapped" />
       );
-    },
-  );
+    };
 
   describe('prop: render', () => {
     it('renders a customized root element with a function', async () => {
