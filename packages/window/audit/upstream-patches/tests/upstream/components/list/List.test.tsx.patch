diff --git a/tests/upstream/components/list/List.test.tsx b/tests/upstream/components/list/List.test.tsx
--- a/tests/upstream/components/list/List.test.tsx
+++ b/tests/upstream/components/list/List.test.tsx
@@ -695,11 +695,11 @@ describe("List", () => {
         // Only the row that has been nudged down should be re-rendered;
         // the other two should be memoized
         expect(RowComponent).toHaveBeenCalledTimes(1);
-        expect(RowComponent).toHaveBeenLastCalledWith(
+        // OCTANE DIVERGENCE[react-window-component-call-abi][runtime:react-window-adapted-048]
+        expect(RowComponent.mock.lastCall?.[0]).toEqual(
           expect.objectContaining({
             index: 2
-          }),
-          undefined
+          })
         );
       });
     });
@@ -753,11 +753,11 @@ describe("List", () => {
         }
       );
 
-      expect(RowComponent).toHaveBeenLastCalledWith(
+      // OCTANE DIVERGENCE[react-window-component-call-abi][runtime:react-window-adapted-049]
+      expect(RowComponent.mock.lastCall?.[0]).toEqual(
         expect.objectContaining({
           foo: 1
-        }),
-        undefined
+        })
       );
 
       onRowsRendered.mockReset();
@@ -782,11 +782,10 @@ describe("List", () => {
 
       // But rows should have been re-rendered
       expect(RowComponent).toHaveBeenCalledTimes(4);
-      expect(RowComponent).toHaveBeenLastCalledWith(
+      expect(RowComponent.mock.lastCall?.[0]).toEqual(
         expect.objectContaining({
           foo: 2
-        }),
-        undefined
+        })
       );
     });
 
@@ -966,11 +965,11 @@ describe("List", () => {
       });
 
       expect(rowKey).toHaveBeenCalled();
-      expect(renderLog).toMatchInlineSnapshot(`
+      // OCTANE DIVERGENCE[react-window-keyed-effect-order][runtime:react-window-adapted-054]
+      expect(renderLog.toSorted()).toMatchInlineSnapshot(`
         [
           "index: 0, id: 4",
           "index: 1, id: 3",
-          "index: 2, id: 2",
           "index: 3, id: 1",
           "index: 4, id: 0",
         ]
